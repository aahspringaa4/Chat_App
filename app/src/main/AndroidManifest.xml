<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.chat_app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name="ui.activity.login.AccountActivity" />
        <activity android:name="ui.activity.ProfileEditActivity" />
        <activity android:name="ui.activity.MypageActivity" />
        <activity android:name="ui.activity.ImageActivity" />
        <activity android:name="ui.activity.ChattingActivity" />
        <activity android:name="ui.activity.login.HomeActivity" />
        <activity android:name="ui.activity.login.JoinActivity" />
        <activity android:name="ui.activity.login.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>